# Live-memory captures: tens of MB each and reproducible by re-running the tool.
# Patterns in a .gitignore are relative to ITS directory, so this must be *.bin --
# a repo-root path here matches nothing, which is how an 80 MB capture got pushed.
*.bin
